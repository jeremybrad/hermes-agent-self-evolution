# Project metadata for C027_hermes-evolution
# Auto-generated by Claude — agents should keep this current

project:
  repo_id: C027_hermes-evolution
  owner: Jeremy Bradford
  last_reviewed: 2026-05-04
  summary: "PC-only Hermes Agent skill-evolution workbench (DSPy + GEPA over WSL hermes-agent)"
  status: active
  series: C
  version: "0.1.0"
  machine_scope: PC
  upstream_fork_of: NousResearch/hermes-agent-self-evolution

folders:
  00_admin: "Administrative files and session continuity"
  00_run: "Easy-button launchers (PowerShell/.cmd)"
  10_docs: "Documentation and guides"
  20_receipts: "Change receipts and session handoffs"
  30_config: "Configuration files (model routing, skill scope, env defaults)"
  40_src: "Source code — evolution package + report generator"
  50_data: "Data directory (gitignored — synthetic datasets, regenerated on demand)"
  60_tests: "Test suite — pytest"
  70_evidence: "Run artifacts (output/) — committed as reproducibility evidence"
  80_reports: "Generated reports (PDFs, validation summaries)"
  90_archive: "Deprecated runs and superseded scripts"

files:
  README.md: "Project overview"
  CLAUDE.md: "Claude Code guidance"
  AGENTS.md: "Generic agent guidance (parallel to CLAUDE.md)"
  CHANGELOG.md: "Change history"
  CONTRIBUTING.md: "Branching and commit conventions"
  META.yaml: "Project metadata (this file)"
  RELATIONS.yaml: "Cross-repo relationships and ownership boundaries"
  ROADMAP.md: "Phase 1.1 → Phase 5 plan"
  WHY_I_CARE.md: "Purpose and motivation"
  PLAN.md: "Detailed engineering plan (kept from upstream)"
  rules_now.md: "Current working rules for active sessions"
  pyproject.toml: "Python project config and dependencies"
  40_src/evolution/skills/evolve_skill.py: "GEPA optimization CLI for SKILL.md files"
  40_src/evolution/core/fitness.py: "Fitness functions (keyword-overlap heuristic + LLMJudge)"
  40_src/evolution/core/constraints.py: "Evolved-variant constraint validator"
  40_src/evolution/core/dataset_builder.py: "Synthetic + golden eval dataset builders"
  40_src/evolution/core/external_importers.py: "Hermes/Claude/Copilot session importers"
  40_src/generate_report.py: "Validation report PDF generator"

services: {}

relates_to:
  - upstream/hermes-agent-self-evolution: "Fork of NousResearch repo — pull upstream improvements when applicable"
  - upstream/hermes-agent: "Skills source — \\\\wsl.localhost\\Ubuntu-22.04\\home\\jbrad\\.hermes\\hermes-agent\\skills (PC) — read by evolve_skill.py"
  - C010_standards: "Workspace conventions and Betty Protocol compliance"
  - C027_hermes-evolution-mac: "FUTURE — likely Mac fork of this repo, configured separately"

maintainer_notes:
  - "PC-only repo. Mac will fork (likely as a separate C-series repo) and diverge in memory system, agent set, and possibly dataset sources."
  - "Hermes runtime (the agent that USES the evolved skills) lives in WSL at ~/.hermes/hermes-agent/ on this machine. This repo is the workbench, not the runtime."
  - "Layout migration on 2026-05-04: evolution/→40_src/evolution/, tests/→60_tests/, output/→70_evidence/runs/, reports/→80_reports/, datasets/→50_data/datasets/. See 20_receipts/2026-05-04_betty_protocol_compliance.md."
  - "Python package name `evolution` is preserved across the move (pyproject.toml uses where=['40_src']). All `from evolution.X import Y` imports continue to work without changes."
  - "Run artifacts under 70_evidence/runs/<skill>/<timestamp>/ are committed as reproducibility evidence even though the eval datasets that drive them are gitignored (synthetic, regenerated on demand)."
  - "Last successful GEPA run: 70_evidence/runs/arxiv/20260424_184914/ (+3.9% holdout, -75% body size on keyword-overlap proxy)."
  - "optuna 4.8.0 is installed in C:\\Python313 globally but NOT declared in pyproject.toml; required by MIPROv2 fallback."
